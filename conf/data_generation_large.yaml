# Large dataset configuration
# Usage: uv run python generate_data.py --config-name data_generation_large

defaults:
  - data_generation

# Override generation settings for large dataset
generation:
  num_samples: 5000
  subset_size: 20

# Higher resolution images
features:
  img_size: 128


