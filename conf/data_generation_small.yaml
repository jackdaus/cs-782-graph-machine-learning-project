# Small dataset configuration - useful for quick testing
# Usage: uv run python generate_data.py --config-name data_generation_small

defaults:
  - data_generation

# Override generation settings for small dataset
generation:
  num_samples: 100
  subset_size: 10

# Use smaller images for faster processing
features:
  img_size: 64

