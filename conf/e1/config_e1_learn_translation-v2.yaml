model:
  embedding_size: 32

training:
  num_epochs: 1000
  batch_size: 32
  lr: 0.001
#  lambda_translation: 0.1 # Weight for translation loss vs rotation loss
#  lambda_frobenius: 0.1 # Weight for Frobenius norm regularization
  scheduler:
    enabled: true
    step_size: 200
    gamma: 0.8
