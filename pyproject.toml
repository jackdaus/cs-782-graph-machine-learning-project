[project]
name = "colmap-experiments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.7",
    "pycolmap>=3.12.6",
]

[dependency-groups]
dev = [
    "ipykernel>=7.0.0",
]
